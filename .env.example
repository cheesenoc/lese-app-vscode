# Copy to .env and fill in values (do NOT commit your .env)
FTP_HOST=ftp.example.com
FTP_PORT=21
FTP_USER=your-username
FTP_PASSWORD=your-password
# FTP security mode:
# - 'false' (default): plain FTP (not recommended)
# - 'true' or 'explicit': explicit FTPS (AUTH TLS)
# - 'implicit': implicit FTPS (TLS from the start)
FTP_SECURE=false
# Minimum TLS version to accept (e.g., TLSv1.2)
FTP_TLS_MIN_VERSION=TLSv1.2
# Remote path where files should be uploaded
FTP_PATH=/html/
# Local directory to upload (defaults to repo root)
FTP_LOCAL_DIR=.
# Set to 'true' to enable verbose FTP debugging
FTP_DEBUG=false
